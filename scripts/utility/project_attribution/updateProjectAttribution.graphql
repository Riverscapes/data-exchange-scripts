mutation updateProjectAttribution($projectId: ID!, $project: ProjectInput!) {
  updateProject(project: $project, projectId: $projectId) {
    id
    attribution {
        organization {
            name
        }
        roles
    }
  }
}